---
- hosts: localhost
  connection: local
  become: true

  tasks:
  - name: Install Ubuntu Packages
    package:
      name:
        - tmux
        - vim-nox

  - name: Install Snaps
    community.general.snap:
      name:
        - bitwarden
        - chromium
        - htop
        - thunderbird
        - pdfarranger
